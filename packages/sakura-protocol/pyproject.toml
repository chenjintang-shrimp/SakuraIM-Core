[project]
name = "sakura-protocol"
version = "0.1.0"
description = "Shared protocol models for SakuraIM"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.13.4",
]

[[tool.uv.index]]
url = "https://mirrors.aliyun.com/pypi/simple/"
default = true
