[tool.uv.workspace]
members = [
    "packages/sakura-protocol",
    "packages/sakura-core",
]

[[tool.uv.index]]
url = "https://mirrors.aliyun.com/pypi/simple/"
default = true

[dependency-groups]
dev = [
    "ruff>=0.15.15",
    "ty>=0.0.40",
]
